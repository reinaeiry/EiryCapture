<Window x:Class="StreamingAgent.App.KeybindsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keybinds" Height="220" Width="360" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Select Region" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="SelectRegionBox" Grid.Column="1" PreviewKeyDown="OnKeyBoxPreviewKeyDown" IsReadOnly="True"/>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Start / Stop" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="StartStopBox" Grid.Column="1" PreviewKeyDown="OnKeyBoxPreviewKeyDown" IsReadOnly="True"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Show / Hide Mirror" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="ShowHideMirrorBox" Grid.Column="1" PreviewKeyDown="OnKeyBoxPreviewKeyDown" IsReadOnly="True"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Clear" Width="70" Margin="0,0,8,0" Click="OnClearClick"/>
            <Button Content="OK" Width="70" Margin="0,0,8,0" Click="OnOkClick"/>
            <Button Content="Cancel" Width="70" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
